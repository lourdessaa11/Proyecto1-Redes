# ============================
# Google Sheets (backend primario)
# ============================

# ID de tu Google Sheet maestro (parte entre /d/ y /edit en la URL).
GOOGLE_SHEETS_MASTER_ID=

# Nombre de la pestaña en la hoja (debe existir). Por defecto usamos "Master".
GOOGLE_SHEETS_TAB=Master


# ============================
# Google Drive (opcional, para futuras extensiones)
# ============================

# Si más adelante adjuntas archivos y guardas en Drive, define aquí la carpeta raíz (folderId).
# Por ahora, puede quedar vacío.
GOOGLE_DRIVE_ROOT_FOLDER_ID=


# ============================
# Microsoft Graph (para reemplazar el mock de correo)
# ============================

# Datos de la App registrada en Azure AD (cuando migres a envío real de correos).
MS_TENANT_ID=
MS_CLIENT_ID=
MS_CLIENT_SECRET=

# Refresh token u otro mecanismo de persistencia que uses en tu flujo OAuth (opcional hasta integrar).
MS_REFRESH_TOKEN=

# Correo del remitente (ej. la cuenta real desde la que enviarás).
MS_SENDER_EMAIL=


# ============================
# Parámetros de alertas
# ============================

# Días antes del vencimiento para generar alertas (por defecto 30).
ALERTS_DAYS_BEFORE=30

# Días de felicitación/post-vencimiento si quieres mensajes especiales (por defecto 0 = inactivo).
ALERTS_DAYS_CONGRATS=0
